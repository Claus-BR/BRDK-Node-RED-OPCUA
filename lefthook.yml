colors: false

pre-commit:
  parallel: true
  jobs:
    - name: format
      glob: '*.{js,html}'
      run: npx prettier --check {staged_files}
      stage_fixed: true
    - name: lint
      glob: '*.js'
      run: npx eslint {staged_files}

